services:
  backend:
    image: planterson/vue-node-backend
    container_name: vue-backend
    build:
      context: .
      dockerfile: Dockerfile
    # ports:
    #   - "3000:3000"
    volumes:
      - ./server.js:/backend/server.js:ro
    entrypoint: ["npm", "run", "dev"]